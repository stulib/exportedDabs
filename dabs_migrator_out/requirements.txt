# Local development + test dependencies only.
# Runtime cluster libraries belong in the bundle's `environments` / `libraries` blocks.

databricks-cli>=0.18
databricks-sdk>=0.20
pytest>=7
pytest-mock>=3
ruff>=0.4
pyyaml>=6
